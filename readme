C++ seocket client example

This is a class sample using websocketpp  C++ header only library

It handles two type co PING: Websocket level and application level.

In case of connectio error,  the example closes and restart websocket connection

it connects adn subscribes to "wss://api.bitfinex.com:443/ws/2"  for the sake of the test



